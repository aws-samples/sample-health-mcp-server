{
  "mcpServers": {
    "aws-health": {
      "command": "python",
      "args": ["-m", "aws_health_mcp.server"],
      "env": {
        "AWS_PROFILE": "default",
        "LOG_LEVEL": "WARNING"
      }
    }
  }
}
