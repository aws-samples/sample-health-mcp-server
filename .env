# AWS Configuration - Using AWS CLI Profile
# Your AWS CLI is already configured, so we'll use that

# Use AWS CLI profile
AWS_PROFILE=default
AWS_DEFAULT_REGION=us-east-1

# Optional: Enable debug logging
# AWS_HEALTH_DEBUG=true
